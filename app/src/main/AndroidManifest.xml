<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:enabled="true"
        android:name=".IngaleApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ingale"
        tools:targetApi="31">
        <activity
            android:screenOrientation="portrait"
            android:name=".main_navigation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Ingale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".core.audio_player.PlayerService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>
        <service android:name=".core.audio_player.actions.PlaybackActionService"/>

        <receiver
            android:exported="false"
            android:name=".core.audio_player.actions.player_action_receivers.PlayerActionSkipToPrevious"/>
        <receiver
            android:exported="false"
            android:name=".core.audio_player.actions.player_action_receivers.PlayerActionTogglePlaybackReceiver"/>
        <receiver
            android:exported="false"
            android:name=".core.audio_player.actions.player_action_receivers.PlayerActionSkipToNextReceiver"/>
        <receiver
            android:exported="false"
            android:name=".core.audio_player.actions.player_action_receivers.PlayerActionStopServiceReceiver"/>
    </application>
</manifest>